# HTTP Adapters

This directory contains HTTP framework adapters for different web frameworks.

## Structure

- `gin/` - Gin framework adapter
- `fiber/` - Fiber framework adapter
- `middleware/` - Common middleware implementations
- `handlers/` - HTTP request handlers

## Responsibilities

- Provide HTTP transport layer
- Handle request/response serialization
- Implement framework-specific middleware
- Route registration and management
